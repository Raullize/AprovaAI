version: '3.8'

services:
  db_test:
    image: postgres:16
    container_name: aprovaai_postgres_test
    environment:
      POSTGRES_DB: aprovaai_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
